# 节点模块库

这个目录包含所有可用于流程编辑器的节点模块。每个节点模块都应该遵循标准接口，以便编辑器能够正确加载和使用它们。

## 模块结构

每个模块应包含以下文件:
- `manifest.json`: 模块的元数据
- `node.js`: 节点的JavaScript实现
- `style.css`: (可选) 节点的自定义样式

## 标准接口

所有节点模块必须实现标准接口以确保与编辑器的兼容性。
